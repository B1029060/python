オブジェクト指向三大要素：カプセル化(Encapsulation)、継承(Inheritance)、ポリモフィズム(Polymorphism)

カプセル化；
    データとそのデータを使った処理を
    ひとまとまりにして
    クラスに持たせる

    メリット：
    ．内部の処理や状態を意識せずにオブジェクトに対する操作
    ．変更をする時にクラスの外に影響が及びにくい

継承：
    親クラスの性質を
    子クラスが引き継ぐ

ポリモフィズム：
    クラスが違っても
    処理を同じように
    呼び出せる